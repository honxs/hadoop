package cn.mastercom.bigdata.util;

public interface IDataDeal
{
	/**
	 *
	 * @param dataType 数据类型，dataType唯一区分一种数据,这里是流式的put，dataType从小到大
	 * @param value 数据的字符串
	 * @return 0 是成功， -1是失败
	 */
	int pushData(int dataType, String value);
	
	void statData();
	
	void outData();
}
